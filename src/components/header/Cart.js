import menuIcon from '../../icons/menu.svg'

const Cart = () => {
    return(
        <button className="header-menu">
            <img src={menuIcon} alt="menu" />
        </button>
    )
}

export default Cart;